spring.servlet.multipart.max-file-size=128MB
spring.servlet.multipart.max-request-size=128MB
spring.servlet.multipart.enabled=true

server.port=${PORT}

schedule.database.url=${DB_URL}
schedule.database.name=${DB_NAME}